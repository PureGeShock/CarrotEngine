#pragma once

/*
 * These values are automatically set according to their cmake variables.
 */
#define ENGINE_VERSION "${ENGINE_VERSION}"
#define CURRENT_PLATFORM ${CURRENT_PLATFORM}
#define PROJECT_CONFIGURATION ${PROJECT_CONFIGURATION}
#define WRITE_LOGS_TO_LOGFILE ${WRITE_LOGS_TO_LOGFILE}
#define PROJECT_NAME "${PROJECT_NAME}"
